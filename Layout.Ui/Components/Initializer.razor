@namespace Layout.Ui.Components
@inherits ComponentBase
@implements IDisposable
@*@attribute [PiralExtension("Layout.Initializer")]*@
@*@inject NavigationManager _navigationManager*@
@*@inject MyAuthenticationState _authenticationState*@
    

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();
        //_authenticationState.OnStateChange += StateHasChanged;
        //_authenticationState.Initialize();
    }
    void IDisposable.Dispose()
    {
        //_authenticationState.OnStateChange -= StateHasChanged;
    }
}